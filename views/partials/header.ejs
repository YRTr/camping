<!DOCTYPE html>
<html>

<head>
  <title>Camping site</title>
  <meta charset="utf-8" />
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
    integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous" />
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.0/css/all.css"
    integrity="sha384-lZN37f5QGtY3VHgisS14W3ExzMWZxybE1SJSEsQp9S+oqd12jhcu+A56Ebc1zFSJ" crossorigin="anonymous" />
  <link rel="stylesheet" href="/stylesheets/main.css" />
  <style>
    .jumbotron {
      margin-top: 60px !important;
    }

    a:hover #info {
      text-decoration: none;
    }
  </style>
</head>

<body>
  <nav id="mainNavbar" class="navbar navbar-light bg-secondary fixed-top navbar-expand-md">
    <div class="container-fluid">
      <a id="brandName" class="navbar-brand mr-auto" href="/"><span><i class="fas fa-campground"></i></span>Campin</a>
      <button class="navbar-toggler" data-toggle="collapse" data-target="navLinks" aria-label="toggle-navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="d-flex justify-content-end mx-2">
        <div class="collapse navbar-collapse" id="navLinks">
          <ul class="navbar-nav">
            <% if(!currentUser){ %>
            <li class="nav-item">
              <a href="/login" class="nav-link">
                <div class="btn btn-secondary">Login</div>
              </a>
            </li>
            <li class="nav-item">
              <a href="/register" class="nav-link">
                <div class="btn btn-secondary">Sign Up</div>
              </a>
            </li>
            <% } else { %>
            <li class="nav-item">
              <a href="/logout" class="nav-link">
                <div class="btn btn-secondary">Logout</div>
              </a>
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </nav>

  <div class="container" style="margin-top: 65px;">
    <% if(error && error.length > 0) { %>
    <div class="alert alert-danger" role="alert">
      <%= error %>
    </div>
    <% } %>
    <% if(success && success.length > 0){ %>
    <div class="alert alert-success" role="alert">
      <%= success %>
    </div>
    <% } %>
  </div>